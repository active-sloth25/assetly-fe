export const MAIN_APP_UI_ROUTES = {
  MAIN: 'assetly',
};

export const AUTHENTICATION_UI_ROUTES = {
  MAIN: 'account',
  SIGN_IN: 'sign-in',
  SIGN_UP: 'sign-up',
  VERIFY_OTP: 'verify-otp',
};

export const DASHBOARD_UI_ROUTES = {
  MAIN: 'dashboard',
};

export const ADMINS_UI_ROUTES = Object.freeze({
  MAIN: 'admins',
});

export const CONFIGURATIONS_UI_ROUTES = Object.freeze({
  MAIN: 'config',
});

export const HOME_PAGE_UI_ROUTES = Object.freeze({
  MAIN: 'home-page',
});

export const NOT_FOUND_UI_ROUTES = Object.freeze({
  MAIN: 'not-found',
});
